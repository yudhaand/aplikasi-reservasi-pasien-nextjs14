/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["cdn.pixabay.com", "i.imgflip.com", "imgflip.com", "plus.unsplash.com", "images.unsplash.com"],
  },
};

export default nextConfig;
